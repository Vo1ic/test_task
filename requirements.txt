# ──────────────────────────────────────────────────────────────────────────────
# Google API
# ──────────────────────────────────────────────────────────────────────────────
google-api-python-client==2.131.0
google-auth==2.29.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# ──────────────────────────────────────────────────────────────────────────────
# Google Sheets (gspread + форматування)
# ──────────────────────────────────────────────────────────────────────────────
gspread==6.1.2
gspread-formatting==1.1.2

# ──────────────────────────────────────────────────────────────────────────────
# Аудіо — конвертація mp3→wav БЕЗ ffmpeg
# soundfile >= 0.11.0 містить вбудований декодер mp3 через libsndfile
# ──────────────────────────────────────────────────────────────────────────────
soundfile==0.12.1

# ──────────────────────────────────────────────────────────────────────────────
# Розпізнавання мовлення (Google Web Speech API, безкоштовно, без ключів)
# SpeechRecognition >= 3.11.0 підтримує Python 3.13+ (модуль aifc видалено зі stdlib)
# ──────────────────────────────────────────────────────────────────────────────
SpeechRecognition==3.16.1
standard-aifc==3.13.0
audioop-lts==0.2.2

# ──────────────────────────────────────────────────────────────────────────────
# LLM — локальний ШІ-аналіз тексту (llama-cpp-python + GGUF-модель)
# При першому запуску скрипт автоматично завантажує модель Qwen2.5-3B-Instruct
# (~1.9 ГБ) через huggingface_hub. Платних API ключів не потрібно.
#
# ВАЖЛИВО (Windows): llama-cpp-python НЕ встановлюється через pip напряму —
# потрібен pre-built wheel. setup.ps1 робить це автоматично. Або вручну:
#
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# ──────────────────────────────────────────────────────────────────────────────
huggingface_hub==0.23.4
